@model BO.Theme
@using SolutionPrincipale.Extension;

@{
    ViewBag.Title = "Create";
}

<div class="container">
    <h2>Création d'un thème</h2>


    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-horizontal">
            <h4>Thème</h4>
            <hr />
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })

            @Html.CustomEditorFor(model => model.Nom, "text")
            @Html.CustomEditorFor(model => model.Description, "text")

            @Html.CustomButtonSubmit("Créer le thème")
        </div>
    }

    <div>
        @Html.ActionLink("Revenir à la liste des thèmes", "Index", null, new { @class = "btn btn-default" })
    </div>
</div>
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
